#!/bin/sh
# Rocket.Chat - Self-signed HTTPS deployment
# Deploy with: docker compose -f compose.database.yml -f compose.traefik.yml -f compose.yml up -d

REG_TOKEN=
DOMAIN=temptail
ROOT_URL=https://temptail
RELEASE=8.0.1

# TLS - Traefik uses its default self-signed cert when LETSENCRYPT is disabled
LETSENCRYPT_ENABLED=false
LETSENCRYPT_EMAIL=demo@email.com
TRAEFIK_PROTOCOL=https

# Traefik ports (HTTPS only)
TRAEFIK_DASHBOARD_PORT=8080
TRAEFIK_HTTPS_PORT=443

# Grafana (not used, but GRAFANA_PATH must not be / or it hijacks all routes)
GRAFANA_DOMAIN=
GRAFANA_PATH=/grafana

# MongoDB
MONGODB_BIND_IP=127.0.0.1
MONGODB_PORT_NUMBER=27017

# NATS
NATS_PORT_NUMBER=4222
NATS_BIND_IP=127.0.0.1
